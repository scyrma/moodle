# Changelog

## Unreleased
### Changed
- The `$fieldtype` parameter passed to `api::get_datasource_field_sql` should now contain one of the
  column type constants from `core_reportbuilder\local\report\column` if specified

## Previous versions
Changelog was not maintained before version 3.10
